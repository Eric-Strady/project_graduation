{{ form_start(form) }}
	<fieldset>
		<legend>Détails de l'article</legend>
		{{ form_row(form.title) }}
		{{ form_row(form.tag) }}
		{{ form_row(form.content) }}
	</fieldset>

	<fieldset>
		<legend>Image de l'article</legend>
		<div class="row">
			<div class="col-lg-6">{{ form_row(form.image.title) }}</div>
			<div class="col-lg-6">{{ form_row(form.image.extension) }}</div>
		</div>
	</fieldset>
	
	<div class="row mt-4">
		<div class="col-lg-12 text-center">
			<button type="submit" class="btn btn-primary">{{ button }}</button>
		</div>
	</div>
	
{{ form_end(form )}}