{{ form_start(form) }}
	<fieldset>
		<legend>Détails de l'article</legend>
		<div class="row">
			<div class="col-lg-6">{{ form_row(form.title) }}</div>
			<div class="col-lg-6">{{ form_row(form.image_file) }}</div>
		</div>
		{{ form_row(form.category) }}
		{{ form_row(form.content) }}
	</fieldset>
	
	<div class="row submitButton">
		<div class="col-lg-12 text-center">
			<button type="submit" class="btn btn-primary btn-lg">{{ button }}</button>
		</div>
	</div>
	
{{ form_end(form )}}