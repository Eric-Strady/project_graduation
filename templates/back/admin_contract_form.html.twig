{{ form_start(form) }}
	<fieldset>
		<legend>Détails du contrat</legend>
		{{ form_row(form.name) }}
		<div class="row">
			<div class="col-lg-6">{{ form_row(form.starting_season_at) }}</div>
			<div class="col-lg-6">{{ form_row(form.ending_season_at) }}</div>
		</div>
		{{ form_row(form.summary) }}
		{{ form_row(form.description) }}
	</fieldset>
	
	<fieldset>
		<legend>Informations sur le producteur</legend>
		{{ form_row(form.grower_name) }}
		<div class="row">
			<div class="col-lg-6">{{ form_row(form.grower_gps_lat) }}</div>
			<div class="col-lg-6">{{ form_row(form.grower_gps_lng) }}</div>
		</div>
	</fieldset>

	<fieldset>
		<legend>Image du contrat</legend>
		<div class="row">
			<div class="col-lg-6">{{ form_row(form.image.title) }}</div>
			<div class="col-lg-6">{{ form_row(form.image.extension) }}</div>
		</div>
	</fieldset>
	
	<div class="row mt-4">
		<div class="col-lg-12 text-center">
			<button type="submit" class="btn btn-primary">{{ button }}</button>
		</div>
	</div>
	
{{ form_end(form )}}