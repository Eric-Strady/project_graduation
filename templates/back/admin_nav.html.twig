<nav class="navbar navbar-expand-lg navbar-light fixed-top">
	<a class="navbar-brand" href="{{ path('admin.home') }}">Interface d'administration</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav ml-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('home') }}">Aller sur le site</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('admin.contracts') }}">Les contrats</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('admin.posts') }}">Le blog</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('admin.about') }}">A propos</a>
			</li>
			<li class="nav-item dropdown">
		        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">AMAP Pré-Vert</a>
		        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
					<a class="dropdown-item" href="{{ path('admin.account.reset.email') }}">Changer l'identifiant</a>
					<a class="dropdown-item" href="{{ path('admin.account.reset.password') }}">Changer le mot de passe</a>
					<a class="dropdown-item" href="{{ path('logout') }}">Se déconnecter</a>
		        </div>
		    </li>
		</ul>
	</div>
</nav>